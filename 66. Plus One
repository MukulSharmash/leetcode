class Solution:
    def plusOne(self, l: List[int]) -> List[int]:
        s=""
        for i in range(len(l)):
            s=s+str(l[i])
        a=int(s)
        b=str(a+1)
        c=[]
        for i in b:
            c.append(int(i))
        return c
