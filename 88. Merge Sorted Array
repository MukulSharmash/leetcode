class Solution:
    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:
        a=len(nums1)
        for i in range(m,a):
            nums1.pop()
        for i in range(n):
            nums1.append(nums2[i])
        nums1.sort()
